[project]
name = "redis-ha-with-sentinels"
version = "0.1.0"
description = "Redis HA with Sentinels"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "python-dotenv>=1.0.1",
    "redis>=6.1.1",
]
